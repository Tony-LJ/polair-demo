/*
@descri: TypeScript 项目的核心配置文件，用于告诉 TypeScript 编译器（tsc）如何处理项目中的 TypeScript/JavaScript 文件，包括编译规则、类型检查范围、输出目录、模块解析方式等。
         简单来说，它是 TypeScript 项目的 “编译说明书”，同时也是 WebStorm/VS Code 等 IDE 识别 TS 类型、提供语法提示的核心依据。
@author: Tony
@date:
*/
{
  "compilerOptions": {
    "target": "esnext",
    "module": "esnext",
    "strict": true,
    "jsx": "preserve",
    "moduleResolution": "node",
    "esModuleInterop": true,
    "skipLibCheck": true,
    "allowSyntheticDefaultImports": true,
    "sourceMap": true,
    "resolveJsonModule": true,
    "lib": ["esnext", "dom"],
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.d.ts",
    "src/**/*.tsx",
    "src/**/*.vue",
    "src/vite-env.d.ts" // 确保声明文件被包含
  ],
  "exclude": ["node_modules", "dist"]
}
